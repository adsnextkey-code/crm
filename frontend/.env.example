# Required when the frontend is hosted separately from the backend (e.g. frontend on
# Hostinger, backend on Vercel). Must point to the deployed backend's /api path.
VITE_API_URL=https://your-backend.vercel.app/api
